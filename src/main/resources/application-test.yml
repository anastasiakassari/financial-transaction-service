spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fts_test
    username: postgres
    password: password
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

